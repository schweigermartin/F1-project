{
  "name": "@f1/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AWS CDK infrastructure for the F1 project (Phase 0+).",
  "scripts": {
    "build": "echo \"infra build placeholder — esbuild for lambda assets added in Phase 1 T11\"",
    "lint": "echo \"infra lint placeholder — covered by root eslint .\"",
    "test": "echo \"infra test placeholder — snapshot tests added in T11\"",
    "typecheck": "tsc --noEmit",
    "cdk": "cdk",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "aws-cdk": "^2.1125.0",
    "tsx": "^4.22.3"
  },
  "dependencies": {
    "@f1/shared": "workspace:*",
    "aws-cdk-lib": "^2.257.0",
    "constructs": "^10.6.0"
  }
}
