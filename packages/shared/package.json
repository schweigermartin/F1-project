{
  "name": "@f1/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared types, Zod schemas, and S3 path helpers — single source of truth across apps and infra.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./ws-token": "./src/ws-token.ts"
  },
  "scripts": {
    "build": "echo \"shared build placeholder — TS build added in T2\"",
    "lint": "echo \"shared lint placeholder\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "vitest": "^4.1.7"
  }
}
