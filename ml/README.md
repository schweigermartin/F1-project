# ml/

Python-Workspace für ML (Phase 3+). Nicht Teil des pnpm-Workspaces — eigene Toolchain (uv / pip + venv).

Wird in Phase 3 mit `requirements.txt` (oder `pyproject.toml`), Notebooks und Trainings-Scripts gefüllt.
